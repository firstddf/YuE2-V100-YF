# 变更记录

## 0.1

首个成形版本:**在 Tesla V100(SM70)上跑通的 YuE2 生成 + MuScriptor 参考曲分析,配自建中文界面与 DSH 技能。**

### 引擎

- 自编译 [audio.cpp](https://github.com/0xShug0/audio.cpp)(dev 分支),Visual Studio 17 2022,
  单架构 `70-real`,Release。
- **编入两个模型家族:`yue2`(生成)+ `muscriptor`(音频转谱)**。
- 本地补丁 2 处(见 `docs/v100-patches.md`):
  - `external/ggml/src/ggml-cuda/fattn.cu` —— Volta 上 flash attention 落到无设备代码的 MMA 内核
  - `src/models/yue2/*` —— ABC 乐谱导出(5 个文件,49 行新增)
- 权重:`Yue2-3B-GGUF`(Q8_0 + VAE f16,4.22 GiB)、`MuScriptor-Small-GGUF`(f32,392.8 MB)。

### 界面(6 个页签)

| 页签 | 内容 |
|---|---|
| 🎼 单首生成 | 风格五要素分类下拉(英文标签 + 中文解释)、歌词分段助手、时长换算、简谱、指标 |
| 📦 批量出歌 | 多段歌词排队 |
| 🗂 历史记录 | **按生成时间倒序 + 时间列**;点表格行即试听;重启不丢 |
| 📜 **日志历史** | 任务日志(引擎完整输出)+ `logs` 目录文件浏览;「最新失败任务」一键定位 |
| 🔍 **参考曲分析** | librosa 快速画像 / MuScriptor 音符级转谱;标签一键并入风格框 |
| ❓ 说明与限制 | 全部实测边界 |

### 生成

- 时长**没有参数**,由歌词长度决定;**实测精确换算 1 秒 = 25 语义 token**(7 组全部命中)。
- `cot=off/melody/full` 三种规划路线;`melody`/`full` 产出 ABC 乐谱,界面自动换算**简谱**
  (调号 / 八度点 / 减时线 / 附点,声部按小节对齐),并导出 `score.jianpu.txt`。
- 同 seed + 同参数 = **逐字节相同**(已验),做 A/B 必须换 seed 或换参数。
- 实测实时倍率 **1.4–2×**;峰值显存 7985 MiB / 16384。

### 参考曲分析

- `POST /api/analyze`:`level=basic`(librosa,约 9 秒,纯 CPU)/ `level=full`
  (+ MuScriptor 转谱,约 21 秒,占 GPU ~0.9 GB)。
- **GPU 互斥**:`full` 时若有生成任务在跑直接返回 409,而不是排队抢显存。
- 标签建议只覆盖**情绪/氛围**类;**语言与曲风需用户补**(语言决定唱什么语言,测不出来)。
- 转谱实测:**5.2×–18.9× 实时**(耗时由音符密度决定,不由时长决定)。
- 与 librosa 交叉印证:BPM 116.8 vs 117.5(0.6%)、79.9 vs 80.7(1.0%)。

### 诊断

- 失败时界面**必然摊开三样**:失败原因、**实际下发的参数表**、引擎日志尾部(日志框自动可见)。
- 服务端**预校验** `abc_*` / `semantic_*` 范围,越界 400 并指明参数,不再等加载完模型才失败。
- 修复:`abc_max_tokens=0` 撞 `max>=min(32)`、`semantic_repetition_penalty=0` 撞 `>0` ——
  浏览器里未动过的数字框提交 0 而非空。
- 修复:服务日志 0 字节 —— Python stdout 重定向到文件时块缓冲,启动加 `-u`。

### DSH 技能

- `skills/song-production/`(规范副本)+ `scripts/install-skill.ps1`(安装/校验)。
- 核心价值是**边界表**:气声未达标、段落留空不退场、标签是概率性的、ABC 不等于音频、
  若隐若现只能整轨、agent 听不到音频。

### 已知边界(全部实测)

| 事项 | 事实 |
|---|---|
| 参考音频 / 人声参考 | **不支持**。源码显式拒绝 `audio_input`;实测传 `--voice-ref` 输出字节级一致 |
| 气声 / 喘息 | 只到"哼唱",**没达标** |
| 人声退场 | 必须用 `[Instrumental]`;**段落留空无效** |
| 速度 / 时长参数 | **都没有**。速度只能靠标签;时长靠歌词长度 |
| 人声单独进退 | 需要分轨(HTDemucs/BS-RoFormer),**未编入** |
| 音色替换 / 改词翻唱 | 需要 Seed-VC / ACE-Step,**未编入** |
| 判断手段 | ASR 对无词吟唱无效;乐谱不能判断有无演唱;agent 听不到音频 |

### 环境

Python 3.12.10 / torch 2.8.0+cu128 / CUDA 12.8(锁 sm_70)/ V100-SXM2-16GB /
驱动 **572.83(不要升到 580 以上)** / 20 逻辑核 / D: 剩约 25 GB。
